spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wareworker
    username: postgres
    password: change_me
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: update
  sql:
    init.mode: always